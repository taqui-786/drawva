/**
 * The single shared horizontal bounds used by header, main, and footer so
 * every row's visible content aligns to the exact same left/right edges.
 */
export const CONTAINER = "mx-auto w-full max-w-[1440px] px-6 md:px-10";